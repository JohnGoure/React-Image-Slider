This is a generic material UI image slider.

To install clone this repository then type npm install while inside the directory.

After the dependencies are installed type npm start to run the dev server.